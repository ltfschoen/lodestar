{
  "extends": "../../tsconfig",
  "include": ["src","test"],
  "compilerOptions": {
    "target": "es2018",
    "typeRoots": [
      "../../node_modules/.pnpm/@types",
      "../../node_modules/.pnpm/libp2p-ts/types",
      "./types"
    ],
    "outDir": "lib",
    /* Redirect output structure to the directory. */
    "rootDirs": ["./src","./test"]
    /* Specify the root directory of input files. Use to control the output directory structure with --outDir. */
  }
}
